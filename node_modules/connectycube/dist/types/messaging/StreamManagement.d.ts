import { XMPPClient } from './xmpp';
import { Chat } from '../types';
export default class StreamManagement {
    readonly xmlns: string;
    private enabled;
    private clientProcessedStanzasCounter;
    private clientSentStanzasCounter;
    private lastAck;
    private unackedQueue;
    private xmppClient;
    private originalSend;
    sentMessageCallback: Chat.OnMessageSentListener;
    constructor();
    enable(connection: XMPPClient): void;
    removeElementHandler(): void;
    addElementHandler(): void;
    send(stanza: Chat.XmlElement, message?: object): void;
    private incomingStanzaHandler;
}
//# sourceMappingURL=StreamManagement.d.ts.map