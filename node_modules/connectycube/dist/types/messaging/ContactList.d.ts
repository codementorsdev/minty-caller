import ChatHelpers from './Helpers';
import { XMPPClient } from './xmpp';
import { Chat } from '../types';
export default class ContactList {
    readonly xmlns: string;
    contacts: Chat.ContactList;
    helpers: ChatHelpers;
    xmppClient: XMPPClient;
    stanzasCallbacks: Chat.StanzasCallbacks;
    constructor(options: {
        helpers: ChatHelpers;
        xmppClient: XMPPClient;
        stanzasCallbacks: Chat.StanzasCallbacks;
    });
    get(): Promise<Chat.ContactList>;
    add(params: Chat.ContactListItem | number): Promise<void>;
    confirm(params: Chat.ContactListItem | number): Promise<void>;
    reject(userId: number): Promise<void>;
    updateName(params: Chat.ContactListItem): Promise<void>;
    remove(userId: number): Promise<void>;
    sendSubscriptionPresence(params: Chat.ContactListSubscriptionPresence): void;
}
//# sourceMappingURL=ContactList.d.ts.map